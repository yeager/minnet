[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "minnet"
version = "0.1.0"
description = "Working memory training games for autism and ADHD"
license = "GPL-3.0-or-later"

[tool.setuptools.packages.find]
where = ["."]
